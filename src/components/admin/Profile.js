import React, { Component } from 'react'

export default class Profile extends Component {
  render() {
    return (
      <div>"Hi admin" with a drop down menu, contains the options of: view admin info and sign out option (when the sign out pressed, redirect to the user home page)</div>
    )
  }
}
