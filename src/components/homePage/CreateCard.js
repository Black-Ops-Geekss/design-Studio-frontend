import React, { Component } from 'react'

export default class CreateCard extends Component {
  render() {
    return (
      <div>a div contain a route navigate to the createOwnCards Component so the user can add a url </div>
    )
  }
}
