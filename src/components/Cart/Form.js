import React, { Component } from 'react'

export default class Form extends Component {
  render() {
    return (
      <div>a form shown when the user confirm their order, to enter their information, and then save the info in the data base</div>
    )
  }
}
